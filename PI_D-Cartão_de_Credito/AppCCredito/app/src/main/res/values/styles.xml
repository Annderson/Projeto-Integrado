<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/preto</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LoginFontes">
        <item name="android:textSize">@dimen/login_fontes</item>
        <item name="android:layout_marginLeft">@dimen/margem_lados</item>
        <item name="android:layout_marginRight">@dimen/margem_lados</item>
        <item name="android:textColor">@color/preto</item>
    </style>

</resources>
